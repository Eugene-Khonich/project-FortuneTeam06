description: 'Conflict error'
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: string
          example: 409
        message:
          type: string
          example: 'This email is already in use'
