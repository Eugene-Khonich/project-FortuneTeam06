description: 'Bad request error'
content:
  application/json:
    schema:
      type: object
      required:
        - message
        - status
      properties:
        message:
          type: string
          example: 'Bad request'
        status:
          type: integer
          example: 400
